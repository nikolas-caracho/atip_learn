    // Crie uma função chamada verificarEstoque que recebe 3 parâmetros, quantidade, baixo e crítico,
    // e retorna 'Estoque normal' se a quantidade for maior que o valor de baixo, 'Estoque baixo' se a
    // quantidade for menor ou igual ao valor de baixo e maior que o valor de crítico, e 'Estoque crítico'
    // se a quantidade for menor ou igual ao valor de crítico. Utilize essa função para verificar o estoque de
    // 100 unidades, com valor baixo de 50 unidades e valor crítico de 20 unidades.

function verificarEstoque() {

}